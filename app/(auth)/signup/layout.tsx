import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Create Account | Leadivo",
  description: "Create your free Leadivo account and start selling online in minutes. No coding needed.",
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
